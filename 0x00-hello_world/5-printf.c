#include <stdio.h>
/**
 * main - Program will print a phrase followed by a new line
 * Return: The program will return a zero if compiled correctly
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
