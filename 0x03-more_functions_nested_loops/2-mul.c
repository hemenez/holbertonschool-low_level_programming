#include "holberton.h"
/**
 * mul - function will multiply two integers
 * @a: represents first integer to be multiplied
 * @b: represents second integer to be multiplied
 * Return: function will return result
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
