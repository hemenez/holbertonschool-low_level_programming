#include "holberton.h"
/**
 * add - function will add two integers
 * Return: function will return value of addition
 * @a: holds value of first integer
 * @b: holds value of second integer
 */

int add(int a, int b)
{
	int v;

	v = (a + b);
	return (v);
}
